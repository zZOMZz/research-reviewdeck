{
  "name": "@diffdeck/core",
  "version": "1.0.0",
  "description": "",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc && esbuild src/index.ts --bundle --outfile=dist/index.mjs --format=esm --platform=node",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@diffdeck/shared": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "@diffdeck/typescript-config": "workspace:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
